import { style } from '@vanilla-extract/css';

export const banner = style({
	width: '100%',
	maxWidth: 1400,
	margin: '0 auto',
	padding: '0 20px',
});

export const image = style({
	width: '100%',
	height: 'auto',
	maxWidth: '100%',
});
